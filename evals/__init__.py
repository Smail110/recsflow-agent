"""Оценочный harness RecAgent: сценарии, оракул, симулятор, метрики.

Пакет живёт ОТДЕЛЬНО от ``src/recagent`` нарочно. Оракул и симулятор не должны
импортировать код агента (``matches``, ``rule_parse``, ``explain``, ``Agent``),
иначе критерий успеха снова станет тавтологией — ровно тем дефектом, из-за
которого прежняя оценка давала ``success_rate = 1.0`` (``docs/EVAL-PLAN.md`` §1).

Разделение проверяется тестом ``tests/unit/test_oracle_independence.py``, который
читает исходники этого пакета и запрещает в них импорты модулей агента.

Состав:
  * ``oracle.py``   — критерий приёма ответа (theta + сказанные ограничения)
  * ``scenarios.py``— генератор D3: реплики, theta, ожидаемый исход
  * ``simulator.py``— детерминированный симулятор пользователя (LLM только
                      перефразирует реплики, решения принимает код)
  * ``metrics.py``  — агрегация: success rate, число уточнений, доля
                      неподтверждённых утверждений, латентность, bootstrap CI
  * ``run.py``      — прогон лестницы B0..B3 и сохранение результатов
  * ``thresholds.yaml`` / ``baseline.json`` — зафиксированные ДО прогона пороги
"""
