.venv/
__pycache__/
.pytest_cache/
.env
data/*.json
report/*-local.json
.streamlit/secrets.toml
dist/

# Build / packaging artifacts
src/*.egg-info/
*.egg-info/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
artifacts/

# Downloaded source datasets (links, not files - see docs/DATA-PLAN.md)
data/raw/

# Rebuild dialogue datasets with python -m evals.scenarios.
evals/data/*.jsonl
artifacts-*.log

# Local-only evaluation runs
report/*-local.json

# Local files unrelated to this project (VPN/proxy configs).
# THEY CONTAIN LIVE CREDENTIALS: passwords, private keys, UUIDs.
# Never commit. Created by other tooling in this folder, not by RecAgent.
remnanode_optimized.json
xray_client_fixed.json
*_optimized.json
xray_client_*.json
remnanode_*.json
